import React from 'react';
import PartContainer from '../PartContainer';

const Structure = ({id}) => {

  return (
    <>
    <PartContainer id={id}>
  
    <h1>Bom Structure</h1>
   
    </PartContainer>
    </>
  )
}

export default Structure;